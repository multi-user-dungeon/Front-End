const initialState = {
    moving: false,
    loggingIn: false,
    registering: false,
    user: {}
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer